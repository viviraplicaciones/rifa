rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // PERMITIR TODO (Lectura y Escritura pública)
      // Necesario para que la rifa funcione sin logueo de usuarios
      allow read, write: if true;
    }
  }
}
